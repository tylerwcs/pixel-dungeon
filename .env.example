OPENAI_API_KEY=
# OPENAI_IMAGE_MODEL=gpt-image-2.5-sunburst
# Characters one IP address (in practice, the booth) may generate per 10 minutes.
# CHARACTER_RATE_LIMIT=20

# Required by the Vercel serverless adapter.
BLOB_READ_WRITE_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: use the local mock generator without spending API credits.
# MOCK_CHARACTER_API=1
